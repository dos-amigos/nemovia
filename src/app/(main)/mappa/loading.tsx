import { Skeleton } from "@/components/ui/skeleton";

export default function MappaLoading() {
  return (
    <Skeleton className="w-full rounded-lg h-[calc(100vh-10rem)] lg:h-[calc(100vh-8rem)]" />
  );
}
