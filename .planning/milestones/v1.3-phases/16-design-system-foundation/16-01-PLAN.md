---
phase: 16-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "All text in the app renders in Geist font (not Inter)"
    - "The amber-600/stone-50 palette is completely replaced by a vibrant coral/teal OKLCH palette"
    - "All 12 Shadcn token pairs (primary, secondary, accent, muted, destructive, card, popover, border, input, ring, sidebar, chart) use the new palette values"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Geist font loaded via next/font/google"
      contains: "Geist"
    - path: "src/app/globals.css"
      provides: "New OKLCH palette values in :root, updated @theme inline font-sans"
      contains: "font-geist-sans"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-geist-sans set by next/font, consumed by @theme inline --font-sans"
      pattern: "font-geist-sans"
    - from: "src/app/globals.css :root"
      to: "src/app/globals.css @theme inline"
      via: "CSS custom properties mapped to Tailwind color variables"
      pattern: "--color-primary: var\\(--primary\\)"
---

<objective>
Replace the Inter font with Geist and swap the entire amber/stone OKLCH palette for a vibrant coral/teal palette across all Shadcn CSS variable tokens.

Purpose: Transform the app's visual identity from dated amber/stone to a modern, vibrant aesthetic -- the foundation that all subsequent visual changes build on.
Output: Updated layout.tsx (Geist font) and globals.css (new palette + font reference).
</objective>

<execution_context>
@C:/Users/boxwe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/boxwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-design-system-foundation/16-RESEARCH.md

<interfaces>
<!-- Current layout.tsx -- font import to be replaced -->
From src/app/layout.tsx:
```typescript
import { Inter } from "next/font/google";
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});
// body className: `${inter.className} antialiased`
```

<!-- Current globals.css -- :root values to be replaced, @theme inline font ref to update -->
From src/app/globals.css:
```css
:root {
  --primary: oklch(0.666 0.179 58.318);  /* amber-600 -- REPLACE */
  --accent: oklch(0.527 0.154 150.069);  /* green-700 -- REPLACE */
  /* ... all 25+ variables use old palette hues ... */
}
@theme inline {
  --font-sans: var(--font-inter);  /* REPLACE with --font-geist-sans */
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Inter to Geist font</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <action>
In src/app/layout.tsx:
1. Replace `import { Inter } from "next/font/google"` with `import { Geist } from "next/font/google"`
2. Replace the `inter` const with:
   ```typescript
   const geist = Geist({
     variable: "--font-geist-sans",
     subsets: ["latin"],
   });
   ```
3. In the body tag, replace `${inter.className}` with `${geist.className}`
4. Keep everything else unchanged (metadata, viewport, Providers, etc.)

In src/app/globals.css:
1. In the `@theme inline` block, replace `--font-sans: var(--font-inter);` with `--font-sans: var(--font-geist-sans);`
2. Do NOT touch the :root color values in this task (that's Task 2)

Geist is a variable font (weight 100-900), no weight specification needed. subsets: ["latin"] is sufficient for Italian content.
  </action>
  <verify>
    <automated>cd C:/Users/boxwe/Documents/GitHub/nemovia && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>layout.tsx imports Geist instead of Inter, globals.css @theme inline references --font-geist-sans, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Replace entire OKLCH palette and Shadcn tokens</name>
  <files>src/app/globals.css</files>
  <action>
Replace ALL color values in the `:root` block of globals.css with the new coral/teal palette. Keep `--radius: 0.625rem` unchanged. Replace every OKLCH value:

```css
:root {
  --radius: 0.625rem;

  /* Background and Foreground -- cool neutral base */
  --background: oklch(0.985 0.005 260);
  --foreground: oklch(0.145 0.015 260);

  --card: oklch(0.995 0.002 260);
  --card-foreground: oklch(0.145 0.015 260);

  --popover: oklch(0.995 0.002 260);
  --popover-foreground: oklch(0.145 0.015 260);

  /* Primary -- warm coral/vermillion */
  --primary: oklch(0.637 0.237 25.5);
  --primary-foreground: oklch(0.985 0.005 260);

  /* Secondary -- light cool gray */
  --secondary: oklch(0.960 0.008 260);
  --secondary-foreground: oklch(0.205 0.015 260);

  /* Muted -- subtle cool gray */
  --muted: oklch(0.960 0.008 260);
  --muted-foreground: oklch(0.556 0.015 260);

  /* Accent -- teal */
  --accent: oklch(0.600 0.155 185);
  --accent-foreground: oklch(0.985 0.005 260);

  /* Destructive -- true red */
  --destructive: oklch(0.577 0.245 27.3);
  --destructive-foreground: oklch(0.985 0 0);

  /* Borders -- cool gray */
  --border: oklch(0.905 0.008 260);
  --input: oklch(0.905 0.008 260);
  --ring: oklch(0.637 0.237 25.5);

  /* Charts */
  --chart-1: oklch(0.637 0.237 25.5);
  --chart-2: oklch(0.600 0.155 185);
  --chart-3: oklch(0.700 0.150 85);
  --chart-4: oklch(0.650 0.200 330);
  --chart-5: oklch(0.700 0.180 145);

  /* Sidebar */
  --sidebar: oklch(0.985 0.005 260);
  --sidebar-foreground: oklch(0.145 0.015 260);
  --sidebar-primary: oklch(0.637 0.237 25.5);
  --sidebar-primary-foreground: oklch(0.985 0.005 260);
  --sidebar-accent: oklch(0.960 0.008 260);
  --sidebar-accent-foreground: oklch(0.205 0.015 260);
  --sidebar-border: oklch(0.905 0.008 260);
  --sidebar-ring: oklch(0.637 0.237 25.5);
}
```

Remove ALL the old-palette comments (/* stone-50 */, /* amber-600 */, etc.) and replace with the new semantic comments shown above.

Also update the shimmer gradient highlight in the `@layer utilities` block:
- Replace `oklch(0.95 0.001 106)` with `oklch(0.95 0.005 260)` to match the new cool neutral hue.

Do NOT change the `@theme inline` block structure (it maps vars to Tailwind colors correctly).
Do NOT change the `@layer base`, `@keyframes shimmer`, or `@media (prefers-reduced-motion)` blocks structurally.
Do NOT introduce any hsl() values -- keep everything in OKLCH.
  </action>
  <verify>
    <automated>cd C:/Users/boxwe/Documents/GitHub/nemovia && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 25+ OKLCH CSS custom properties in :root use the new coral/teal palette. Shimmer gradient uses hue 260. Zero references to old amber (hue 58) or old stone (hue 106) values remain in globals.css. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with no errors
2. In globals.css, no OKLCH value contains hue 58 (old amber) or hue 106 (old stone) or hue 150 (old green)
3. `@theme inline` references `--font-geist-sans` not `--font-inter`
4. layout.tsx imports `Geist` not `Inter`
</verification>

<success_criteria>
- Build passes with zero errors
- All text renders in Geist font (visible in browser DevTools computed font-family)
- The :root block contains exclusively new palette OKLCH values (coral primary hue ~25.5, teal accent hue ~185, cool neutral hue ~260)
- All 12 Shadcn token pairs are updated consistently with proper foreground contrast
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-system-foundation/16-01-SUMMARY.md`
</output>
