---
phase: 12-responsive-desktop-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/TopNav.tsx
  - src/components/layout/BottomNav.tsx
  - src/app/(main)/layout.tsx
  - src/app/(main)/mappa/MappaClientPage.tsx
  - src/app/(main)/mappa/loading.tsx
autonomous: true
requirements: [DESK-01, DESK-03]

must_haves:
  truths:
    - "User on lg+ screens sees a sticky top navigation bar with Nemovia brand, Home/Cerca/Mappa links"
    - "User on lg+ screens does NOT see the BottomNav"
    - "User on mobile (<lg) sees the BottomNav and does NOT see the TopNav"
    - "User on desktop sees responsive container padding that scales with breakpoints (px-4 on mobile, sm:px-6, lg:px-8)"
    - "User on desktop sees no dead whitespace at page bottom (pb-20 removed when BottomNav hidden)"
    - "Map page fills correct height on both mobile and desktop (accounting for different nav heights)"
  artifacts:
    - path: "src/components/layout/TopNav.tsx"
      provides: "Desktop navigation bar component"
      min_lines: 25
      exports: ["TopNav"]
    - path: "src/components/layout/BottomNav.tsx"
      provides: "Mobile bottom nav with lg:hidden"
      contains: "lg:hidden"
    - path: "src/app/(main)/layout.tsx"
      provides: "Layout shell with both navs and responsive padding"
      contains: "TopNav"
    - path: "src/app/(main)/mappa/MappaClientPage.tsx"
      provides: "Map container with responsive height calc"
      contains: "lg:"
  key_links:
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/layout/TopNav.tsx"
      via: "import and render"
      pattern: "import.*TopNav"
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/layout/BottomNav.tsx"
      via: "import and render"
      pattern: "import.*BottomNav"
---

<objective>
Create the desktop navigation bar and update the layout shell for responsive behavior across all breakpoints.

Purpose: Users on tablet/desktop currently see a BottomNav designed for mobile. This plan creates a TopNav for lg+ screens, hides BottomNav on desktop via CSS, and adds responsive container padding for better desktop spacing.

Output: TopNav.tsx component, updated BottomNav with lg:hidden, updated layout.tsx with responsive padding and both navs, responsive map height calculation.
</objective>

<execution_context>
@C:/Users/boxwe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/boxwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-responsive-desktop-layout/12-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/layout/BottomNav.tsx:
```typescript
"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { Home, Search, Map } from "lucide-react";
import { cn } from "@/lib/utils";

const tabs = [
  { href: "/", label: "Home", icon: Home },
  { href: "/cerca", label: "Cerca", icon: Search },
  { href: "/mappa", label: "Mappa", icon: Map },
] as const;
```

From src/app/(main)/layout.tsx (CURRENT):
```typescript
import { BottomNav } from "@/components/layout/BottomNav";

export default function MainLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <div className="min-h-screen bg-background pb-20">
      <main className="mx-auto max-w-7xl px-4 py-4">{children}</main>
      <BottomNav />
    </div>
  );
}
```

From src/app/(main)/mappa/MappaClientPage.tsx (CURRENT):
```typescript
// Container uses: className="-mx-4 -mt-4" style={{ height: "calc(100vh - 5rem)" }}
// The 5rem accounts for BottomNav (4rem/h-16) + top padding (1rem/py-4)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TopNav and update navigation swap in layout</name>
  <files>src/components/layout/TopNav.tsx, src/components/layout/BottomNav.tsx, src/app/(main)/layout.tsx</files>
  <action>
1. Create `src/components/layout/TopNav.tsx`:
   - "use client" component using `usePathname()` for active state (same pattern as BottomNav)
   - Reuse the same `tabs` array definition as BottomNav: Home(/), Cerca(/cerca), Mappa(/mappa) with same icons
   - Root `<nav>` with className `"hidden lg:block sticky top-0 z-50 border-b border-border bg-background/95 backdrop-blur"`
   - Inner container: `"mx-auto flex h-14 max-w-7xl items-center justify-between px-8"`
   - Left side: Link to "/" with text "Nemovia", className `"text-lg font-bold text-primary"`
   - Right side: flex row of tab links, each with:
     - className using cn(): base `"flex items-center gap-2 rounded-lg px-3 py-2 text-sm transition-colors focus-visible:ring-[3px] focus-visible:ring-ring/50"`, active `"text-primary font-medium bg-primary/5"`, inactive `"text-muted-foreground hover:text-foreground hover:bg-muted"`
     - Icon (h-4 w-4) + label span
   - Active detection: `pathname === href` (exact match, same as BottomNav)
   - Export as named export `TopNav`

2. Update `src/components/layout/BottomNav.tsx`:
   - Add `lg:hidden` to the root `<nav>` className
   - Current: `"fixed bottom-0 left-0 right-0 z-50 border-t border-border bg-background pb-safe"`
   - Updated: `"fixed bottom-0 left-0 right-0 z-50 border-t border-border bg-background pb-safe lg:hidden"`
   - No other changes

3. Update `src/app/(main)/layout.tsx`:
   - Add import: `import { TopNav } from "@/components/layout/TopNav";`
   - Change root div className from `"min-h-screen bg-background pb-20"` to `"min-h-screen bg-background pb-20 lg:pb-0"` (remove BottomNav padding on desktop)
   - Add `<TopNav />` as first child of root div (before `<main>`)
   - Update main className from `"mx-auto max-w-7xl px-4 py-4"` to `"mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-4"` (responsive padding scaling per DESK-01)
   - Keep BottomNav render as-is (it hides itself via lg:hidden)
  </action>
  <verify>
    <automated>cd /c/Users/boxwe/Documents/GitHub/nemovia && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>TopNav.tsx exists with hidden lg:block, BottomNav has lg:hidden, layout.tsx renders both navs with responsive padding and pb-20 lg:pb-0. No test regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Update map page responsive height calculation</name>
  <files>src/app/(main)/mappa/MappaClientPage.tsx, src/app/(main)/mappa/loading.tsx</files>
  <action>
1. Update `src/app/(main)/mappa/MappaClientPage.tsx`:
   - The map container currently uses `className="-mx-4 -mt-4"` and `style={{ height: "calc(100vh - 5rem)" }}`
   - The 5rem = BottomNav (h-16 = 4rem) + top padding (py-4 = 1rem)
   - On desktop: no BottomNav, TopNav is h-14 (3.5rem), padding is py-4 (1rem) = 4.5rem total
   - Update the negative margins to be responsive: className `"-mx-4 -mt-4 sm:-mx-6 lg:-mx-8"` (matching the responsive padding from layout.tsx)
   - Replace inline style with Tailwind arbitrary value: use a style that works for both breakpoints
   - Mobile height: `calc(100vh - 5rem)` (BottomNav h-16 + py-4)
   - Desktop height: `calc(100vh - 4.5rem)` (TopNav h-14 + py-4)
   - Implementation: keep the mobile height as inline style, add a responsive CSS approach.
   - Simplest approach: use `style={{ height: "calc(100vh - 5rem)" }}` and add className `"lg:!h-[calc(100vh-4.5rem)]"` to let Tailwind override at lg breakpoint
   - Note: The `!` important modifier is needed because inline styles have higher specificity than classes. Alternatively, remove inline style and use only Tailwind classes: `"h-[calc(100vh-5rem)] lg:h-[calc(100vh-4.5rem)]"`
   - PREFERRED: Remove the inline `style` entirely and use className `"-mx-4 -mt-4 sm:-mx-6 lg:-mx-8 h-[calc(100vh-5rem)] lg:h-[calc(100vh-4.5rem)]"`

2. Update `src/app/(main)/mappa/loading.tsx`:
   - Mirror the responsive height from MappaClientPage
   - Current: `<Skeleton className="w-full rounded-lg" style={{ height: "calc(100vh - 10rem)" }} />`
   - The 10rem accounts for BottomNav + header + padding. On desktop, adjust similarly.
   - Update to: remove inline style, use className `"w-full rounded-lg h-[calc(100vh-10rem)] lg:h-[calc(100vh-8rem)]"` (desktop has no BottomNav padding, saving ~2rem)
  </action>
  <verify>
    <automated>cd /c/Users/boxwe/Documents/GitHub/nemovia && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>MappaClientPage uses Tailwind responsive height classes (no inline style), mappa/loading.tsx skeleton mirrors responsive height. Map fills available viewport on both mobile and desktop. No test regressions.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes with no regressions
- TopNav.tsx file exists with correct structure
- BottomNav.tsx contains `lg:hidden`
- layout.tsx imports and renders TopNav, has responsive padding classes, has `pb-20 lg:pb-0`
- MappaClientPage uses Tailwind responsive height (no inline style)
- mappa/loading.tsx skeleton has responsive height
</verification>

<success_criteria>
- TopNav visible at lg+ breakpoint (1024px+), BottomNav hidden
- BottomNav visible below lg, TopNav hidden
- Container padding scales: px-4 (mobile) -> sm:px-6 -> lg:px-8
- No dead whitespace at bottom on desktop (pb-20 removed at lg)
- Map page fills correct height on both mobile and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/12-responsive-desktop-layout/12-01-SUMMARY.md`
</output>
