---
phase: 12-responsive-desktop-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/animations/StaggerGrid.tsx
  - src/components/sagra/SagraCard.tsx
  - src/components/home/ProvinceSection.tsx
  - src/components/home/HeroSection.tsx
  - src/components/map/MapView.tsx
  - src/components/detail/SagraDetail.tsx
  - src/app/(main)/loading.tsx
  - src/app/(main)/cerca/loading.tsx
  - src/app/(main)/sagra/[slug]/loading.tsx
autonomous: true
requirements: [DESK-02, DESK-04, DESK-05, SKEL-02]

must_haves:
  truths:
    - "User on tablet sees 2-column card grids, on desktop sees 3-4 column grids"
    - "User on desktop sees sagra detail with image+map on the left and info on the right (side-by-side)"
    - "User hovering a map marker on desktop sees the sagra name in a tooltip without clicking"
    - "User sees skeleton loading shapes that match the responsive grid at every breakpoint (1 col mobile, 2 cols tablet, 3-4 cols desktop)"
    - "Province grid shows 2 cols on mobile, 3 on sm, 4 on lg+"
    - "Hero section scales text and padding up on larger screens"
  artifacts:
    - path: "src/components/animations/StaggerGrid.tsx"
      provides: "Responsive grid wrapper with lg/xl column classes"
      contains: "lg:grid-cols-3"
    - path: "src/components/detail/SagraDetail.tsx"
      provides: "Side-by-side detail layout on desktop"
      contains: "lg:grid-cols-2"
    - path: "src/components/map/MapView.tsx"
      provides: "Map markers with hover tooltips"
      contains: "Tooltip"
    - path: "src/app/(main)/loading.tsx"
      provides: "Home skeleton with responsive grid classes"
      contains: "lg:grid-cols-3"
    - path: "src/app/(main)/cerca/loading.tsx"
      provides: "Cerca skeleton with responsive grid classes"
      contains: "lg:grid-cols-3"
    - path: "src/app/(main)/sagra/[slug]/loading.tsx"
      provides: "Detail skeleton with side-by-side layout on desktop"
      contains: "lg:grid-cols-2"
  key_links:
    - from: "src/components/animations/StaggerGrid.tsx"
      to: "src/components/sagra/SagraGrid.tsx"
      via: "SagraGrid wraps StaggerGrid"
      pattern: "StaggerGrid"
    - from: "src/components/map/MapView.tsx"
      to: "react-leaflet"
      via: "Tooltip import"
      pattern: "import.*Tooltip.*react-leaflet"
    - from: "src/app/(main)/loading.tsx"
      to: "src/components/animations/StaggerGrid.tsx"
      via: "mirrors responsive grid classes"
      pattern: "grid-cols-1.*sm:grid-cols-2.*lg:grid-cols-3"
---

<objective>
Make all content components responsive for multi-column layouts on tablet and desktop, add map marker tooltips, restructure the detail page for side-by-side viewing, and update all skeleton loaders to match.

Purpose: Desktop users currently see single/double-column layouts designed for mobile. This plan adds proper responsive grid scaling, a side-by-side detail layout, map hover tooltips, and ensures skeleton loading states match the content layout at every breakpoint to prevent layout shift.

Output: Responsive StaggerGrid, responsive ProvinceSection, scaled HeroSection, SagraCard image optimization, MapView with Tooltip, side-by-side SagraDetail, and all 3 skeleton loading files updated.
</objective>

<execution_context>
@C:/Users/boxwe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/boxwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-responsive-desktop-layout/12-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/animations/StaggerGrid.tsx (CURRENT):
```typescript
interface StaggerGridProps {
  children: React.ReactNode;
  className?: string;  // default: "grid grid-cols-1 gap-4 sm:grid-cols-2"
}
export function StaggerGrid({ children, className = "grid grid-cols-1 gap-4 sm:grid-cols-2" }: StaggerGridProps)
```

From src/components/sagra/SagraGrid.tsx (CURRENT -- passes no className):
```typescript
export function SagraGrid({ children }: SagraGridProps) {
  return <StaggerGrid>{children}</StaggerGrid>;
}
```

From src/components/sagra/SagraCard.tsx (CURRENT sizes):
```typescript
<Image src={sagra.image_url} alt={sagra.title} fill className="object-cover" sizes="(max-width: 640px) 100vw, 50vw" />
```

From src/components/home/ProvinceSection.tsx (CURRENT grid):
```typescript
<div className="grid grid-cols-2 gap-2">
```

From src/components/home/HeroSection.tsx (CURRENT):
```typescript
<section className="rounded-2xl bg-gradient-to-br from-amber-50 via-orange-50 to-green-50 px-6 py-8">
  <h1 className="text-2xl font-bold text-foreground">
```

From src/components/map/MapView.tsx (CURRENT imports):
```typescript
import { MapContainer, TileLayer, Marker, Popup, useMap } from "react-leaflet";
// Tooltip NOT imported yet
```

From src/components/detail/SagraDetail.tsx (CURRENT root structure):
```typescript
<div className="space-y-6">
  {/* Hero image: className="relative -mx-4 -mt-4 h-48 w-[calc(100%+2rem)]" */}
  {/* Title & info: FadeIn wrapper */}
  {/* Tags: FadeIn wrapper */}
  {/* Description: FadeIn wrapper */}
  {/* Mini map: FadeIn wrapper, h-48 */}
  {/* Action buttons: FadeIn wrapper */}
  {/* Source link: FadeIn wrapper */}
</div>
```

From src/lib/queries/types.ts (MapMarkerData for tooltip):
```typescript
// MapMarkerData has .title property for tooltip text
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Responsive grids, card image sizes, hero scaling, and map tooltips</name>
  <files>src/components/animations/StaggerGrid.tsx, src/components/sagra/SagraCard.tsx, src/components/home/ProvinceSection.tsx, src/components/home/HeroSection.tsx, src/components/map/MapView.tsx</files>
  <action>
1. Update `src/components/animations/StaggerGrid.tsx` (DESK-02):
   - Change default className from `"grid grid-cols-1 gap-4 sm:grid-cols-2"` to `"grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"`
   - This affects all consumers: WeekendSection (via SagraGrid), SearchResults (via SagraGrid)
   - No other changes needed -- SagraGrid passes no custom className

2. Update `src/components/sagra/SagraCard.tsx` (DESK-02 support):
   - Update Image `sizes` attribute from `"(max-width: 640px) 100vw, 50vw"` to `"(max-width: 640px) 100vw, (max-width: 1024px) 50vw, (max-width: 1280px) 33vw, 25vw"`
   - This matches the new grid: 1 col (100vw), 2 cols (50vw), 3 cols (33vw), 4 cols (25vw)

3. Update `src/components/home/ProvinceSection.tsx` (DESK-02):
   - Change grid className from `"grid grid-cols-2 gap-2"` to `"grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-4"`
   - 7 Veneto provinces will flow naturally: 4 cols on lg shows 2 rows (4+3)

4. Update `src/components/home/HeroSection.tsx` (DESK-01 support):
   - Scale up text and padding for desktop. Update section className:
   - From: `"rounded-2xl bg-gradient-to-br from-amber-50 via-orange-50 to-green-50 px-6 py-8"`
   - To: `"rounded-2xl bg-gradient-to-br from-amber-50 via-orange-50 to-green-50 px-6 py-8 lg:px-10 lg:py-12"`
   - Update h1 className from `"text-2xl font-bold text-foreground"` to `"text-2xl font-bold text-foreground lg:text-3xl"`
   - Update subtitle p: add `"lg:text-lg"` to existing classes

5. Update `src/components/map/MapView.tsx` (DESK-05):
   - Add `Tooltip` to the react-leaflet import: `import { MapContainer, TileLayer, Marker, Popup, Tooltip, useMap } from "react-leaflet";`
   - Inside the `<Marker>` component, add `<Tooltip>{sagra.title}</Tooltip>` as a child, BEFORE the existing `<Popup>`:
   ```tsx
   <Marker key={sagra.id} position={position}>
     <Tooltip>{sagra.title}</Tooltip>
     <Popup>
       <MapMarkerPopup sagra={sagra} />
     </Popup>
   </Marker>
   ```
   - The Tooltip component from react-leaflet shows on hover by default and hides on mouse leave. No additional configuration needed.
  </action>
  <verify>
    <automated>cd /c/Users/boxwe/Documents/GitHub/nemovia && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>StaggerGrid default is 1/2/3/4 cols across breakpoints. SagraCard image sizes match grid. ProvinceSection has 2/3/4 cols. HeroSection scales text/padding on desktop. MapView markers show Tooltip on hover. No test regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Side-by-side detail layout and breakpoint-aware skeletons</name>
  <files>src/components/detail/SagraDetail.tsx, src/app/(main)/loading.tsx, src/app/(main)/cerca/loading.tsx, src/app/(main)/sagra/[slug]/loading.tsx</files>
  <action>
1. Update `src/components/detail/SagraDetail.tsx` (DESK-04):
   - Restructure into side-by-side layout on desktop using CSS grid.
   - Change root div className from `"space-y-6"` to `"space-y-6 lg:grid lg:grid-cols-2 lg:gap-8 lg:space-y-0"`
   - Group LEFT column content (hero image + mini map) into a `<div className="space-y-6">`:
     - Hero image block (the `relative -mx-4 -mt-4 h-48 w-[calc(100%+2rem)]` div)
     - Mini map block (the FadeIn with "Posizione" heading and map)
   - Group RIGHT column content into a `<div className="space-y-6">`:
     - Title and location/date info (FadeIn)
     - Tags section (FadeIn)
     - Description section (FadeIn)
     - Action buttons row (FadeIn)
     - Source link (FadeIn)
   - Fix hero image bleed on desktop (Pitfall 2 from research):
     - Current hero: `className="relative -mx-4 -mt-4 h-48 w-[calc(100%+2rem)]"`
     - Add responsive overrides: `className="relative -mx-4 -mt-4 h-48 w-[calc(100%+2rem)] lg:mx-0 lg:mt-0 lg:w-full lg:h-64 lg:rounded-xl lg:overflow-hidden"`
     - The image should be contained within its grid column on desktop, with rounded corners
   - Increase mini map height on desktop: change the map container from `"h-48"` to `"h-48 lg:h-64"` for more map visibility in the left column
   - The mini map should also have `lg:sticky lg:top-20` on the left column wrapper so the map stays visible while scrolling the right column content

2. Update `src/app/(main)/loading.tsx` (SKEL-02 -- home skeleton):
   - Weekend section card grid: change from `"grid grid-cols-1 gap-4 sm:grid-cols-2"` to `"grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"` (mirrors StaggerGrid)
   - Increase skeleton count from 4 to 8 so desktop users see 2 full rows in a 4-column grid
   - Province section grid: change from `"grid grid-cols-2 gap-2"` to `"grid grid-cols-2 gap-2 sm:grid-cols-3 lg:grid-cols-4"` (mirrors ProvinceSection)

3. Update `src/app/(main)/cerca/loading.tsx` (SKEL-02 -- cerca skeleton):
   - Results card grid: change from `"grid grid-cols-1 gap-4 sm:grid-cols-2"` to `"grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4"` (mirrors StaggerGrid)
   - Increase skeleton count from 4 to 8

4. Update `src/app/(main)/sagra/[slug]/loading.tsx` (SKEL-02 -- detail skeleton):
   - Restructure to mirror SagraDetail side-by-side layout:
   - Change root div className from `"space-y-4"` to `"space-y-4 lg:grid lg:grid-cols-2 lg:gap-8 lg:space-y-0"`
   - Left column `<div className="space-y-4">`: image skeleton (h-48 lg:h-64 rounded-xl) + mini map skeleton (h-48 lg:h-64 rounded-xl)
   - Right column `<div className="space-y-4">`: title skeleton + location skeleton + date skeleton + description skeletons + tags skeleton + action buttons skeleton
  </action>
  <verify>
    <automated>cd /c/Users/boxwe/Documents/GitHub/nemovia && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>SagraDetail shows side-by-side layout on lg+ with image+map left, info right. Hero image contained within grid column on desktop (no bleed). All 3 skeleton files mirror their page's responsive grid classes. Skeleton count increased to 8 for desktop grid density. No test regressions.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes with no regressions
- StaggerGrid.tsx default className includes `lg:grid-cols-3 xl:grid-cols-4`
- SagraCard.tsx Image sizes updated for 4-column layout
- ProvinceSection.tsx grid includes `sm:grid-cols-3 lg:grid-cols-4`
- HeroSection.tsx has `lg:text-3xl` and `lg:px-10 lg:py-12`
- MapView.tsx imports and renders `Tooltip` inside each `Marker`
- SagraDetail.tsx uses `lg:grid lg:grid-cols-2` with left (image+map) and right (info) columns
- SagraDetail.tsx hero image has `lg:mx-0 lg:mt-0 lg:w-full lg:rounded-xl`
- Home loading.tsx card grid matches StaggerGrid responsive classes, 8 skeletons
- Cerca loading.tsx card grid matches StaggerGrid responsive classes, 8 skeletons
- Detail loading.tsx mirrors SagraDetail side-by-side layout
</verification>

<success_criteria>
- Card grids show 1 col on mobile, 2 on sm, 3 on lg, 4 on xl
- Province grid shows 2/3/4 columns across breakpoints
- Hero section text and padding scale up on desktop
- Map markers show sagra title tooltip on hover (no click needed)
- Detail page shows side-by-side layout on lg+ (image+map left, info right)
- Detail hero image is contained (no bleed) in desktop grid column
- All skeleton loaders match their page's responsive layout at every breakpoint
</success_criteria>

<output>
After completion, create `.planning/phases/12-responsive-desktop-layout/12-02-SUMMARY.md`
</output>
