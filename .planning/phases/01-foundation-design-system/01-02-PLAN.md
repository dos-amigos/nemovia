---
phase: 01-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/layout/BottomNav.tsx
  - src/app/(main)/layout.tsx
  - src/app/(main)/page.tsx
  - src/app/(main)/cerca/page.tsx
  - src/app/(main)/mappa/page.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02

user_setup:
  - service: vercel
    why: "Deployment hosting for the Next.js app"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Same value as .env.local -- add in Vercel Dashboard -> Project Settings -> Environment Variables"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Same value as .env.local -- add in Vercel Dashboard -> Project Settings -> Environment Variables"
    dashboard_config:
      - task: "Import GitHub repository as a new Vercel project"
        location: "https://vercel.com/new -> Import Git Repository"
      - task: "Add Supabase environment variables"
        location: "Vercel Dashboard -> Project Settings -> Environment Variables"

must_haves:
  truths:
    - "Mobile layout shell renders with BottomNav showing Home, Cerca, Mappa tabs"
    - "Active tab is highlighted with amber-600 primary color"
    - "Placeholder pages exist for all three routes (/, /cerca, /mappa)"
    - "Content does not get hidden behind the fixed BottomNav (pb-20 padding)"
    - "App is deployed on Vercel and accessible via public URL"
  artifacts:
    - path: "src/components/layout/BottomNav.tsx"
      provides: "Mobile bottom navigation with 3 tabs"
      min_lines: 25
      exports: ["BottomNav"]
    - path: "src/app/(main)/layout.tsx"
      provides: "Main layout wrapper with BottomNav and content padding"
      min_lines: 10
    - path: "src/app/(main)/page.tsx"
      provides: "Homepage placeholder with brand styling"
      min_lines: 10
    - path: "src/app/(main)/cerca/page.tsx"
      provides: "Search page placeholder"
      min_lines: 5
    - path: "src/app/(main)/mappa/page.tsx"
      provides: "Map page placeholder"
      min_lines: 5
  key_links:
    - from: "src/components/layout/BottomNav.tsx"
      to: "next/navigation"
      via: "usePathname() for active tab detection"
      pattern: "usePathname"
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/layout/BottomNav.tsx"
      via: "import and render"
      pattern: "import.*BottomNav"
    - from: "src/components/layout/BottomNav.tsx"
      to: "src/app/globals.css"
      via: "text-primary class resolving to amber-600 CSS variable"
      pattern: "text-primary"
---

<objective>
Build the mobile-first layout shell with BottomNav navigation and placeholder pages, then deploy to Vercel.

Purpose: Complete the Phase 1 deliverable -- a publicly accessible, deployed app with the brand design system visible in a mobile layout shell. This proves the full stack works end-to-end (Next.js + Tailwind + shadcn/ui + Vercel).
Output: BottomNav component, 3 placeholder pages in a route group layout, deployed Vercel URL.
</objective>

<execution_context>
@C:/Users/boxwe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/boxwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan needs -->

From src/app/globals.css (brand theme):
- --primary: oklch(0.666 0.179 58.318) -> used via `text-primary`, `bg-primary`
- --muted-foreground: oklch(0.553 0.013 58.071) -> used via `text-muted-foreground`
- --background: oklch(0.985 0.001 106.423) -> used via `bg-background`
- --border: oklch(0.923 0.003 48.717) -> used via `border-border`
- --foreground: oklch(0.216 0.006 56.043) -> used via `text-foreground`

From src/lib/utils.ts (shadcn/ui utility):
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BottomNav component and mobile layout shell with placeholder pages</name>
  <files>
    src/components/layout/BottomNav.tsx,
    src/app/(main)/layout.tsx,
    src/app/(main)/page.tsx,
    src/app/(main)/cerca/page.tsx,
    src/app/(main)/mappa/page.tsx
  </files>
  <action>
1. Create `src/components/layout/BottomNav.tsx` as a client component ("use client"):
   - Import `Link` from next/link, `usePathname` from next/navigation
   - Import `Home`, `Search`, `Map` icons from lucide-react
   - Import `cn` from `@/lib/utils`
   - Define tabs array: `[{ href: "/", label: "Home", icon: Home }, { href: "/cerca", label: "Cerca", icon: Search }, { href: "/mappa", label: "Mappa", icon: Map }]`
   - Render a `<nav>` with classes: `fixed bottom-0 left-0 right-0 z-50 border-t border-border bg-background pb-safe`
   - Inside nav: `<div className="flex h-16 items-center justify-around">`
   - Map over tabs, render each as a `<Link>` with:
     - Active detection: `pathname === href` (exact match)
     - Active styles: `text-primary font-medium`
     - Inactive styles: `text-muted-foreground hover:text-foreground`
     - Layout: `flex flex-col items-center gap-1 px-3 py-2 text-xs transition-colors`
     - Icon: `<Icon className="h-5 w-5" />`
     - Label: `<span>{label}</span>`
   - Export the component as named export: `export function BottomNav()`

2. Create `src/app/(main)/layout.tsx` -- Route group layout wrapping BottomNav:
   - Import `BottomNav` from `@/components/layout/BottomNav`
   - Render: outer div with `min-h-screen bg-background pb-20` (pb-20 prevents content clipping behind fixed nav)
   - Main content area: `<main className="mx-auto max-w-lg px-4 py-4">{children}</main>`
   - Render `<BottomNav />` after main
   - max-w-lg constrains content width on tablets/desktop for a mobile-optimized feel

3. Move the default page to the route group. Delete `src/app/page.tsx` if it exists (from create-next-app scaffolding) and create `src/app/(main)/page.tsx`:
   - Heading: `<h1 className="text-2xl font-bold text-foreground">Scopri le sagre del Veneto</h1>`
   - Subtext: `<p className="text-muted-foreground">Trova sagre ed eventi gastronomici nella tua zona.</p>`
   - 3 placeholder skeleton cards: `<div className="h-48 rounded-lg bg-card border border-border animate-pulse" />`
   - Wrap in `<div className="space-y-6 pt-4">`

4. Create `src/app/(main)/cerca/page.tsx` -- Search page placeholder:
   - Heading: "Cerca sagre"
   - Subtitle text: "Filtra per provincia, tipo cucina, date e altro."
   - A placeholder div simulating a search bar: `<div className="h-12 rounded-lg bg-card border border-border" />`
   - 2 placeholder skeleton cards below

5. Create `src/app/(main)/mappa/page.tsx` -- Map page placeholder:
   - Heading: "Mappa delle sagre"
   - Subtitle: "Esplora le sagre del Veneto sulla mappa."
   - A large placeholder div simulating a map: `<div className="h-[60vh] rounded-lg bg-card border border-border flex items-center justify-center text-muted-foreground">Mappa in arrivo</div>`

ALL pages go inside the (main) route group so they share the BottomNav layout. Final structure:
```
src/app/(main)/layout.tsx        -- BottomNav layout
src/app/(main)/page.tsx          -- Home (/)
src/app/(main)/cerca/page.tsx    -- Search (/cerca)
src/app/(main)/mappa/page.tsx    -- Map (/mappa)
```

6. Delete the default `src/app/page.tsx` from create-next-app if it still exists (to avoid route conflicts with the (main) route group's page.tsx).
  </action>
  <verify>
    <automated>cd C:/Users/boxwe/Documents/GitHub/nemovia && pnpm build && node -e "const fs=require('fs'); const files=['src/components/layout/BottomNav.tsx','src/app/(main)/layout.tsx','src/app/(main)/page.tsx','src/app/(main)/cerca/page.tsx','src/app/(main)/mappa/page.tsx']; files.forEach(f => { if(!fs.existsSync(f)) { console.error('MISSING: '+f); process.exit(1); } }); console.log('All layout files exist')"</automated>
  </verify>
  <done>
    - `pnpm build` succeeds
    - BottomNav renders 3 tabs (Home, Cerca, Mappa) with lucide-react icons
    - Active tab uses `text-primary` (amber-600 brand color)
    - All 3 placeholder pages exist inside (main) route group
    - Homepage shows "Scopri le sagre del Veneto" heading with skeleton cards
    - Main content has `pb-20` so nothing is hidden behind BottomNav
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify mobile layout and deploy to Vercel</name>
  <action>
Human verifies the mobile layout shell and deploys the app to Vercel.

What was built: Complete mobile-first layout shell with BottomNav navigation (Home/Cerca/Mappa tabs), brand-colored active state, and placeholder pages for all three routes. Ready for Vercel deployment.

How to verify:
1. Run `pnpm dev` and open http://localhost:3000 on your phone (or use Chrome DevTools mobile view -- iPhone SE or iPhone 14 preset)
2. Verify the BottomNav is fixed at the bottom with 3 tabs: Home, Cerca, Mappa
3. Tap/click each tab -- the active tab text and icon should turn amber/orange (amber-600 primary)
4. Check that the background is warm stone-50 (off-white, not pure white)
5. On the Home tab, verify "Scopri le sagre del Veneto" heading and pulsing skeleton cards
6. On the Cerca tab, verify "Cerca sagre" heading with search bar placeholder
7. On the Mappa tab, verify "Mappa delle sagre" heading with map placeholder area
8. Scroll the home page -- content should NOT disappear behind the BottomNav
9. DEPLOYMENT: When satisfied with local preview:
   a. Push to GitHub: `git push origin master`
   b. Go to https://vercel.com/new and import the repository
   c. Add environment variables (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY)
   d. Deploy and verify the public URL works
10. Share the Vercel URL to confirm deployment is working
  </action>
  <verify>User confirms visual layout is correct and shares deployed Vercel URL</verify>
  <done>User types "approved" with Vercel URL confirming mobile layout works and app is deployed</done>
</task>

</tasks>

<verification>
- All 3 routes render correctly: `/`, `/cerca`, `/mappa`
- BottomNav active state uses brand primary color (amber-600)
- Background uses brand bg color (stone-50)
- Content is not clipped behind fixed BottomNav
- App is deployed and accessible on Vercel
</verification>

<success_criteria>
- Mobile layout shell is complete with working tab navigation
- Brand design system (amber/green/stone) is visually applied
- App is deployed on Vercel and accessible via public URL
- All Phase 1 requirements (UI-01, UI-02, UI-03) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-02-SUMMARY.md`
</output>
