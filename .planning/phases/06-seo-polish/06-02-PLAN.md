---
phase: 06-seo-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/loading.tsx
  - src/app/(main)/cerca/loading.tsx
  - src/app/(main)/mappa/loading.tsx
  - src/app/(main)/sagra/[slug]/loading.tsx
  - src/components/ui/EmptyState.tsx
  - src/components/home/WeekendSection.tsx
  - src/components/home/ProvinceSection.tsx
  - src/components/search/SearchResults.tsx
autonomous: true
requirements:
  - SEO-05
  - SEO-06

must_haves:
  truths:
    - "Navigating to homepage shows skeleton placeholders while data loads"
    - "Navigating to /cerca shows skeleton placeholders while search runs"
    - "Navigating to /mappa shows skeleton placeholder while map data loads"
    - "Navigating to /sagra/[slug] shows skeleton placeholder while detail loads"
    - "Search with impossible filters shows an empty state with icon and actionable message"
    - "Weekend section with no sagre shows a styled empty state (not just plain text)"
    - "Province section with no counts shows a styled empty state"
  artifacts:
    - path: "src/app/(main)/loading.tsx"
      provides: "Homepage loading skeleton"
      min_lines: 10
    - path: "src/app/(main)/cerca/loading.tsx"
      provides: "Search page loading skeleton"
      min_lines: 10
    - path: "src/app/(main)/mappa/loading.tsx"
      provides: "Map page loading skeleton"
      min_lines: 5
    - path: "src/app/(main)/sagra/[slug]/loading.tsx"
      provides: "Detail page loading skeleton"
      min_lines: 10
    - path: "src/components/ui/EmptyState.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState"]
  key_links:
    - from: "src/app/(main)/loading.tsx"
      to: "src/components/sagra/SagraCardSkeleton.tsx"
      via: "import SagraCardSkeleton"
      pattern: "SagraCardSkeleton"
    - from: "src/components/home/WeekendSection.tsx"
      to: "src/components/ui/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
    - from: "src/components/search/SearchResults.tsx"
      to: "src/components/ui/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
---

<objective>
Add loading skeletons for every route and polished empty states for data-dependent sections.

Purpose: Users see immediate visual feedback during navigation (skeletons) and helpful guidance when no data matches (empty states), rather than blank screens or bare text.
Output: 4 loading.tsx files + reusable EmptyState component + enhanced empty states in WeekendSection, ProvinceSection, SearchResults.
</objective>

<execution_context>
@C:/Users/boxwe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/boxwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-seo-polish/06-RESEARCH.md

<interfaces>
<!-- Existing components to reuse -->

From src/components/sagra/SagraCardSkeleton.tsx:
```typescript
export function SagraCardSkeleton(): JSX.Element;
// Renders a card-shaped skeleton with image, title, description, location, date, tags placeholders
```

From src/components/ui/skeleton.tsx:
```typescript
// Standard shadcn Skeleton component with pulse animation
// Usage: <Skeleton className="h-4 w-full" />
```

From src/components/home/WeekendSection.tsx (current empty state -- plain text):
```typescript
{sagre.length === 0 ? (
  <p className="text-sm text-muted-foreground">
    Nessuna sagra in programma per questo weekend.
  </p>
) : ( ... )}
```

From src/components/search/SearchResults.tsx (current empty state -- plain div):
```typescript
if (sagre.length === 0) {
  return (
    <div className="py-12 text-center">
      <h2 className="text-lg font-semibold">Nessuna sagra trovata</h2>
      <p className="mt-1 text-muted-foreground">
        Prova a cambiare i filtri o amplia il raggio di ricerca.
      </p>
    </div>
  );
}
```

From src/components/home/ProvinceSection.tsx:
```typescript
// Currently has no empty state -- always renders the province grid
```

From src/app/(main)/page.tsx (homepage structure):
```typescript
// HeroSection, QuickFilters, WeekendSection, ProvinceSection
// Skeleton should mirror this layout
```

From src/app/(main)/cerca/page.tsx (search page structure):
```typescript
// h1 "Cerca sagre", SearchFilters, ActiveFilters, ViewToggle, SearchResults
// Skeleton should mirror this layout
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route-level loading skeletons for all 4 routes</name>
  <files>src/app/(main)/loading.tsx, src/app/(main)/cerca/loading.tsx, src/app/(main)/mappa/loading.tsx, src/app/(main)/sagra/[slug]/loading.tsx</files>
  <action>
Create `loading.tsx` files that mirror each page's actual layout structure using Skeleton and SagraCardSkeleton components. Next.js auto-wraps the page in a Suspense boundary when loading.tsx exists.

1. **`src/app/(main)/loading.tsx`** (Homepage skeleton):
   - Hero skeleton: `Skeleton className="h-40 w-full rounded-2xl"` (matches HeroSection gradient box)
   - Quick filters skeleton: horizontal row of 6 pill-shaped skeletons (`h-9 w-20 rounded-full shrink-0`) in an `overflow-hidden flex gap-2` container
   - Weekend section: `Skeleton className="h-6 w-40"` for heading + 4x `SagraCardSkeleton` in a grid matching SagraGrid (`grid grid-cols-1 gap-4 sm:grid-cols-2`)
   - Province section: `Skeleton className="h-6 w-32"` for heading + `grid grid-cols-2 gap-2` with 7 province-row skeletons (`h-12 rounded-lg`)
   - Wrap in `<div className="space-y-8">` to match homepage spacing.

2. **`src/app/(main)/cerca/loading.tsx`** (Search page skeleton):
   - Title skeleton: `Skeleton className="h-7 w-36"` (matches "Cerca sagre" h1)
   - Filters skeleton: `Skeleton className="h-10 w-full rounded-lg"` for filter bar
   - Active filters skeleton: `flex gap-2` with 3 small badge skeletons (`h-6 w-16 rounded-full`)
   - View toggle skeleton: `Skeleton className="h-9 w-24 rounded-md"`
   - Results skeleton: 4x `SagraCardSkeleton` in `grid grid-cols-1 gap-4 sm:grid-cols-2`
   - Wrap in `<div className="space-y-4">` to match search page spacing.

3. **`src/app/(main)/mappa/loading.tsx`** (Map page skeleton):
   - Single full-height skeleton: `Skeleton className="w-full rounded-lg"` with `style={{ height: "calc(100vh - 10rem)" }}` to approximate the fullscreen map area.

4. **`src/app/(main)/sagra/[slug]/loading.tsx`** (Detail page skeleton):
   - Image skeleton: `Skeleton className="h-48 w-full rounded-xl"`
   - Title skeleton: `Skeleton className="h-8 w-3/4"`
   - Location skeleton: `Skeleton className="h-5 w-1/2"`
   - Date skeleton: `Skeleton className="h-5 w-1/3"`
   - Description skeleton: 3 lines (`Skeleton className="h-4 w-full"`, `h-4 w-full`, `h-4 w-2/3`)
   - Tags skeleton: `flex gap-2` with 3 badge skeletons (`h-6 w-16 rounded-full`)
   - Mini map skeleton: `Skeleton className="h-48 w-full rounded-xl"`
   - Action buttons skeleton: `flex gap-3` with 2 button skeletons (`h-10 w-32 rounded-lg`)
   - Wrap in `<div className="space-y-4">`.
  </action>
  <verify>
    <automated>cd C:/Users/boxwe/Documents/GitHub/nemovia && pnpm build</automated>
  </verify>
  <done>All 4 loading.tsx files exist and build succeeds. Each skeleton mirrors its page's visual structure using Skeleton and SagraCardSkeleton components.</done>
</task>

<task type="auto">
  <name>Task 2: EmptyState component + enhance empty states in sections</name>
  <files>src/components/ui/EmptyState.tsx, src/components/home/WeekendSection.tsx, src/components/home/ProvinceSection.tsx, src/components/search/SearchResults.tsx</files>
  <action>
1. **Create `src/components/ui/EmptyState.tsx`**: Reusable empty state component:
   ```typescript
   import { UtensilsCrossed } from "lucide-react";

   interface EmptyStateProps {
     icon?: React.ReactNode;
     title: string;
     description: string;
   }

   export function EmptyState({ icon, title, description }: EmptyStateProps) {
     return (
       <div className="flex flex-col items-center justify-center py-16 text-center">
         <div className="flex h-16 w-16 items-center justify-center rounded-full bg-muted mb-4">
           {icon ?? <UtensilsCrossed className="h-8 w-8 text-muted-foreground" />}
         </div>
         <h2 className="text-lg font-semibold">{title}</h2>
         <p className="mt-1 text-sm text-muted-foreground max-w-xs">{description}</p>
       </div>
     );
   }
   ```

2. **Update `src/components/home/WeekendSection.tsx`**: Replace the plain `<p>` empty state with the EmptyState component:
   - Import `{ Calendar }` from `lucide-react` (already imported) and `{ EmptyState }` from `@/components/ui/EmptyState`.
   - Replace the `sagre.length === 0` branch with: `<EmptyState icon={<Calendar className="h-8 w-8 text-muted-foreground" />} title="Nessuna sagra questo weekend" description="Non ci sono sagre in programma per i prossimi giorni. Torna a controllare presto!" />`
   - Keep all other code unchanged.

3. **Update `src/components/home/ProvinceSection.tsx`**: Add empty state when counts array is empty:
   - Import `{ MapPin }` from `lucide-react` and `{ EmptyState }` from `@/components/ui/EmptyState`.
   - Add a check: if `counts.length === 0`, render `<EmptyState icon={<MapPin className="h-8 w-8 text-muted-foreground" />} title="Nessuna provincia disponibile" description="I dati delle province non sono ancora disponibili." />` instead of the grid.
   - Keep all other code unchanged.

4. **Update `src/components/search/SearchResults.tsx`**: Replace the plain empty state with the EmptyState component:
   - Import `{ Search }` from `lucide-react` and `{ EmptyState }` from `@/components/ui/EmptyState`.
   - Replace the `sagre.length === 0` block with: `<EmptyState icon={<Search className="h-8 w-8 text-muted-foreground" />} title="Nessuna sagra trovata" description="Prova a cambiare i filtri o amplia il raggio di ricerca." />`
   - Keep all other code (mappa view, results rendering) unchanged.
  </action>
  <verify>
    <automated>cd C:/Users/boxwe/Documents/GitHub/nemovia && pnpm build</automated>
  </verify>
  <done>EmptyState component exists with icon, title, description props. WeekendSection, ProvinceSection, and SearchResults use EmptyState with appropriate icons and Italian messages. Build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with no errors
- Each route directory has a loading.tsx file
- EmptyState component is imported and used by 3 section components
</verification>

<success_criteria>
- All 4 routes show skeleton placeholders during server-side data fetching
- Skeletons mirror actual page layout (not generic/mismatched)
- Empty states show centered icon + title + description (not plain text)
- All empty state messages are in Italian
- Existing functionality preserved (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-polish/06-02-SUMMARY.md`
</output>
