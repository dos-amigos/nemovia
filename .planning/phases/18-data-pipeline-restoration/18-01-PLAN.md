---
phase: 18-data-pipeline-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scraper/filters.ts
  - src/lib/scraper/__tests__/filters.test.ts
  - supabase/functions/scrape-sagre/index.ts
  - supabase/migrations/009_filter_recalibration.sql
autonomous: true
requirements: [DATA-01, DATA-03]

must_haves:
  truths:
    - "Legitimate sagre containing secondary activity keywords (mercato, fiera, concerto) are active and visible"
    - "Non-sagra events (standalone passeggiata, carnevale, mostra, concerto) are filtered out at scrape time"
    - "Active sagra count is higher than the ~26 collapse level after filter recalibration"
  artifacts:
    - path: "src/lib/scraper/filters.ts"
      provides: "isNonSagraTitle() whitelist-aware heuristic filter"
      exports: ["isNonSagraTitle"]
    - path: "src/lib/scraper/__tests__/filters.test.ts"
      provides: "Unit tests for isNonSagraTitle covering sagra whitelist and non-sagra patterns"
      contains: "isNonSagraTitle"
    - path: "supabase/functions/scrape-sagre/index.ts"
      provides: "Inline copy of isNonSagraTitle integrated into scrape pipeline"
      contains: "isNonSagraTitle"
    - path: "supabase/migrations/009_filter_recalibration.sql"
      provides: "SQL to re-activate false positives from migration 008 and retroactively apply smarter non-sagra filter"
      contains: "re-activate"
  key_links:
    - from: "src/lib/scraper/filters.ts"
      to: "supabase/functions/scrape-sagre/index.ts"
      via: "inline copy (Deno cannot import from src/)"
      pattern: "isNonSagraTitle"
    - from: "supabase/functions/scrape-sagre/index.ts"
      to: "scrape pipeline filter chain"
      via: "called after isNoiseTitle(), before normalizeRawEvent()"
      pattern: "isNonSagraTitle"
---

<objective>
Recalibrate overly aggressive filters from migration 008 that killed legitimate sagre, and add a smarter isNonSagraTitle() whitelist-aware heuristic filter to the scrape pipeline.

Purpose: The event count collapsed from 735 to ~26 partly because migration 008 keyword filters (fiera, mercato, concerto, etc.) retroactively deactivated legitimate sagre like "Sagra e Fiera del Radicchio". This plan re-activates false positives and replaces the blunt keyword approach with a whitelist-aware filter that only rejects events where the non-sagra keyword is the PRIMARY subject.

Output: Canonical isNonSagraTitle() function with tests, inline copy in scrape-sagre Edge Function, SQL migration to re-activate false positives and apply smarter retroactive cleanup.
</objective>

<execution_context>
@C:/Users/boxwe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/boxwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-data-pipeline-restoration/18-RESEARCH.md

Source files:
@src/lib/scraper/filters.ts
@src/lib/scraper/__tests__/filters.test.ts
@supabase/functions/scrape-sagre/index.ts
@supabase/migrations/008_retroactive_cleanup.sql

<interfaces>
From src/lib/scraper/filters.ts (existing exports to extend):
```typescript
export function isNoiseTitle(title: string): boolean;
export function tryUpgradeImageUrl(imageUrl: string | null, sourceName: string): string | null;
export function isCalendarDateRange(startDate: string | null, endDate: string | null): boolean;
export function isExcessiveDuration(startDate: string | null, endDate: string | null, maxDays?: number): boolean;
export function isPastYearEvent(startDate: string | null, endDate: string | null): boolean;
// NEW: Add isNonSagraTitle() following same pattern (returns true = REJECT)
```

From supabase/functions/scrape-sagre/index.ts (filter chain at line ~636):
```typescript
// Current pipeline filter chain (line 636-643):
if (isNoiseTitle(raw.title)) continue;        // line 636
// >>> INSERT isNonSagraTitle here <<<
const normalized = normalizeRawEvent(raw, source.name);  // line 638
if (isCalendarDateRange(...)) continue;        // line 641
if (isExcessiveDuration(...)) continue;        // line 642
if (isPastYearEvent(...)) continue;            // line 643
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement isNonSagraTitle() with TDD</name>
  <files>src/lib/scraper/filters.ts, src/lib/scraper/__tests__/filters.test.ts</files>
  <behavior>
    - isNonSagraTitle("Passeggiata ecologica") returns true (reject standalone non-sagra)
    - isNonSagraTitle("Carnevale di Venezia") returns true (reject standalone non-sagra)
    - isNonSagraTitle("Mostra di pittura") returns true (reject standalone non-sagra)
    - isNonSagraTitle("Concerto rock in piazza") returns true (reject standalone non-sagra)
    - isNonSagraTitle("Mercatino dell'antiquariato") returns true (reject standalone market)
    - isNonSagraTitle("Sagra e Fiera del Radicchio") returns false (whitelist: contains "sagra")
    - isNonSagraTitle("Festa con Concerto dal Vivo") returns false (whitelist: contains "festa")
    - isNonSagraTitle("Sagra del Mercato Antico") returns false (whitelist: contains "sagra")
    - isNonSagraTitle("Festa Gastronomica del Pesce") returns false (whitelist: food keywords)
    - isNonSagraTitle("Degustazione e Mostra dei Vini") returns false (whitelist: food keywords)
    - isNonSagraTitle("Sagra del Baccala alla Vicentina") returns false (whitelist: food + sagra)
    - isNonSagraTitle("Festa della Polenta") returns false (whitelist: food keywords)
    - isNonSagraTitle("") returns false (empty string, safe default)
    - isNonSagraTitle(null as any) returns false (null-safe)
    - isNonSagraTitle("Teatro Goldoni presenta...") returns true (reject standalone theatre)
    - isNonSagraTitle("Maratona della Citta") returns true (reject sporting event)
  </behavior>
  <action>
    1. RED: Add test cases for isNonSagraTitle() to src/lib/scraper/__tests__/filters.test.ts in a new describe block. Import isNonSagraTitle from the filters module. Run tests -- they must fail (function does not exist yet).

    2. GREEN: Add isNonSagraTitle() to src/lib/scraper/filters.ts following the exact pattern from the research:
       - First check whitelist: if title contains sagra/festa/food keywords, return false (NEVER reject)
       - Whitelist regex: /\b(sagra|sagre|festa|feste|gastronomic|enogastronomic|degustazion|polenta|baccal[aà]|pesce|gnocch|risott|tortel|formagg|asparag|radicchi|funghi|vino|birra|griglia)/i
       - Then check non-sagra patterns: passeggiata/camminata/marcia, carnevale, concerto/concerti/recital, mostra/mostre/esposizione, antiquariato/collezionismo, teatro/teatrale/commedia/spettacolo, maratona/corsa/gara ciclistica/podistica, convegno/conferenza/seminario, cinema/cineforum/proiezione, yoga/fitness/pilates, mercato/mercatino/mercatini
       - Return true only if non-sagra pattern matches AND no whitelist match
       - Handle null/empty input safely (return false)
       - Export the function

    3. REFACTOR: Ensure JSDoc matches the existing filter docstring pattern. Run tests again to confirm green.
  </action>
  <verify>
    <automated>cd /c/Users/boxwe/Documents/GitHub/nemovia && npx vitest run src/lib/scraper/__tests__/filters.test.ts</automated>
  </verify>
  <done>isNonSagraTitle() exported from filters.ts, all test cases pass including whitelist protection for legitimate sagre with secondary activity keywords</done>
</task>

<task type="auto">
  <name>Task 2: SQL migration to recalibrate filters and inline copy to Edge Function</name>
  <files>supabase/migrations/009_filter_recalibration.sql, supabase/functions/scrape-sagre/index.ts</files>
  <action>
    1. Create supabase/migrations/009_filter_recalibration.sql with four sections:

       Section 1 - DRY RUN QUERY (commented out): Count false positives from migration 008 -- events matching mercato/mercatino/mercatini/fiera/concerto/spettacolo/mostra/rassegna keywords BUT also containing sagra/festa/gastronomic in the title AND with start_date >= CURRENT_DATE.

       Section 2 - RE-ACTIVATE FALSE POSITIVES: UPDATE sagre SET is_active = true, updated_at = NOW() WHERE:
         - is_active = false
         - title matches secondary-activity keywords using \y word boundaries (mercato, mercatino, mercatini, fiera, concerto, spettacolo, rassegna, mostra, esposizione, teatro, cinema, convegno, conferenza)
         - AND title ALSO contains sagra/festa/gastronomic indicators using \y word boundaries
         - AND start_date >= CURRENT_DATE (only re-activate future events)
         - AND province IS NOT NULL (only Veneto-gated events)
         - Include dedup guard using NOT EXISTS subquery: check no other active event has matching normalized_title (prevent duplicates from re-activation per research Pitfall 2)

       Section 3 - RETROACTIVE SMART NON-SAGRA CLEANUP: Deactivate active events that match non-sagra patterns BUT do NOT match the whitelist. This mirrors the isNonSagraTitle() logic in SQL:
         - Match non-sagra keywords (passeggiata, camminata, carnevale, concerto, mostra, antiquariato, teatro, maratona, convegno, cinema, yoga, mercato/mercatino)
         - Exclude from deactivation any title also containing sagra/festa/gastronomic/food keywords
         - Use PostgreSQL \y word boundaries

       Section 4 - VERIFICATION QUERIES (commented out): Count active events total, count active by source, list sample re-activated titles.

    2. Add inline copy of isNonSagraTitle() to supabase/functions/scrape-sagre/index.ts:
       - Place the function after the existing isNoiseTitle() function (around line 203), before the date quality filter functions
       - Add header comment: "// --- Section 2b: Non-sagra title filter ---" and "// Inline copy from src/lib/scraper/filters.ts (Deno cannot import from src/)."
       - The function body must be VERBATIM identical to the canonical implementation in filters.ts

    3. Integrate isNonSagraTitle into the filter chain (around line 636-637):
       After `if (isNoiseTitle(raw.title)) continue;` add:
       `if (isNonSagraTitle(raw.title)) continue;`
       This goes BEFORE normalizeRawEvent() because isNonSagraTitle operates on raw title text.

    IMPORTANT: The SQL migration is for manual execution in Supabase SQL Editor (established project pattern since v1.0). Do NOT attempt to run it programmatically. The Edge Function needs manual deployment via Supabase Dashboard.
  </action>
  <verify>
    <automated>cd /c/Users/boxwe/Documents/GitHub/nemovia && npx vitest run src/lib/scraper/__tests__/filters.test.ts</automated>
  </verify>
  <done>SQL migration 009 exists with re-activation and smart cleanup sections. isNonSagraTitle() inline copy present in scrape-sagre Edge Function and integrated into filter chain after isNoiseTitle(). All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/scraper/__tests__/filters.test.ts` -- all tests green including new isNonSagraTitle tests
- `grep -c "isNonSagraTitle" supabase/functions/scrape-sagre/index.ts` returns 2+ (function definition + call site)
- `supabase/migrations/009_filter_recalibration.sql` contains RE-ACTIVATE and CLEANUP sections with dedup guard
- Canonical isNonSagraTitle in filters.ts matches inline copy in Edge Function
</verification>

<success_criteria>
- isNonSagraTitle() passes 16+ test cases covering both rejection and whitelist protection
- SQL migration ready for manual execution to re-activate false positives from migration 008
- Edge Function has isNonSagraTitle() integrated into filter chain between isNoiseTitle and normalizeRawEvent
- No regressions in existing filter tests
</success_criteria>

<output>
After completion, create `.planning/phases/18-data-pipeline-restoration/18-01-SUMMARY.md`
</output>
