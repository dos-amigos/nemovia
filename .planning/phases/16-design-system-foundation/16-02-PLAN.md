---
phase: 16-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/components/home/HeroSection.tsx
  - src/components/map/LocationButton.tsx
  - src/components/search/SearchFilters.tsx
autonomous: false
requirements:
  - UI-04

must_haves:
  truths:
    - "No hardcoded amber, orange, stone, or green Tailwind color classes remain in any component"
    - "HeroSection gradient uses semantic tokens (primary/accent) instead of amber-50/orange-50/green-50"
    - "LocationButton and SearchFilters active states use accent token instead of green-700/green-300/green-50"
    - "All dark: prefixed color classes are removed (dark mode is out of scope)"
  artifacts:
    - path: "src/components/home/HeroSection.tsx"
      provides: "Hero gradient using semantic tokens"
      contains: "from-primary"
    - path: "src/components/map/LocationButton.tsx"
      provides: "Location active state using accent token"
      contains: "bg-accent"
    - path: "src/components/search/SearchFilters.tsx"
      provides: "Geo active button using accent token"
      contains: "text-accent"
  key_links:
    - from: "src/components/home/HeroSection.tsx"
      to: "src/app/globals.css :root --primary"
      via: "Tailwind semantic class from-primary/10"
      pattern: "from-primary"
    - from: "src/components/map/LocationButton.tsx"
      to: "src/app/globals.css :root --accent"
      via: "Tailwind semantic class bg-accent/10"
      pattern: "bg-accent"
    - from: "src/components/search/SearchFilters.tsx"
      to: "src/app/globals.css :root --accent"
      via: "Tailwind semantic class text-accent"
      pattern: "text-accent"
---

<objective>
Replace all hardcoded old-palette Tailwind color classes in components with semantic token references, completing the palette migration. Then visually verify the full redesign.

Purpose: Ensure every component in the app uses the new CSS variable tokens -- no stale amber/green/stone colors leak through.
Output: Updated HeroSection, LocationButton, SearchFilters with semantic color classes.
</objective>

<execution_context>
@C:/Users/boxwe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/boxwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-design-system-foundation/16-RESEARCH.md
@.planning/phases/16-design-system-foundation/16-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, the new palette tokens available via Tailwind classes -->
Available semantic tokens (from globals.css :root after Plan 01):
- primary (coral, hue 25.5) / primary-foreground
- accent (teal, hue 185) / accent-foreground
- muted / muted-foreground
- secondary / secondary-foreground
- destructive / destructive-foreground
- background / foreground
- card / card-foreground
- border, input, ring

Tailwind usage: bg-primary, text-accent, border-accent/30, from-primary/10, etc.
Opacity modifiers work: bg-accent/10 = accent at 10% opacity.

<!-- Files with hardcoded colors to replace (from research inventory) -->
From src/components/home/HeroSection.tsx:
```tsx
<section className="rounded-2xl bg-gradient-to-br from-amber-50 via-orange-50 to-green-50 px-6 py-8 lg:px-10 lg:py-12">
```

From src/components/map/LocationButton.tsx:
```tsx
className="bg-green-50 border-green-300 text-green-700 dark:bg-green-950 dark:border-green-800 dark:text-green-400 shadow-md"
```

From src/components/search/SearchFilters.tsx:
```tsx
className="text-green-700 border-green-300 bg-green-50 dark:text-green-400 dark:border-green-800 dark:bg-green-950"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded color classes in all components</name>
  <files>src/components/home/HeroSection.tsx, src/components/map/LocationButton.tsx, src/components/search/SearchFilters.tsx</files>
  <action>
**HeroSection.tsx** (line 8):
Replace `from-amber-50 via-orange-50 to-green-50` with `from-primary/10 via-primary/5 to-accent/10`

The full className becomes:
```
rounded-2xl bg-gradient-to-br from-primary/10 via-primary/5 to-accent/10 px-6 py-8 lg:px-10 lg:py-12
```

**LocationButton.tsx** (line 30):
Replace `bg-green-50 border-green-300 text-green-700 dark:bg-green-950 dark:border-green-800 dark:text-green-400 shadow-md` with `bg-accent/10 border-accent/30 text-accent shadow-md`

Remove all `dark:` prefixed classes entirely (dark mode is out of scope per REQUIREMENTS.md).

**SearchFilters.tsx** (line 78):
Replace `text-green-700 border-green-300 bg-green-50 dark:text-green-400 dark:border-green-800 dark:bg-green-950` with `text-accent border-accent/30 bg-accent/10`

Remove all `dark:` prefixed classes entirely.

Do NOT modify any other parts of these components (logic, structure, other classes).
Do NOT hardcode new raw color names (like `bg-teal-50`) -- use ONLY semantic tokens.
  </action>
  <verify>
    <automated>cd C:/Users/boxwe/Documents/GitHub/nemovia && npx next build 2>&1 | tail -5 && echo "---GREP CHECK---" && grep -r "amber-\|stone-\|from-green\|to-green\|border-green\|bg-green\|text-green\|from-orange\|via-orange\|dark:bg-green\|dark:border-green\|dark:text-green" src/ --include="*.tsx" --include="*.css" || echo "CLEAN: No old palette references found"</automated>
  </verify>
  <done>Zero hardcoded old-palette color classes remain in src/. All 3 components use semantic tokens. All dark: color overrides removed. Build succeeds. Grep returns zero matches for amber-, stone-, green- color classes in src/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete design system</name>
  <files>none</files>
  <action>
User visually verifies the complete design system transformation across all pages.
  </action>
  <verify>User confirms visual correctness</verify>
  <done>User approves the new design system appearance</done>
  <what-built>Complete design system transformation: Geist font, coral/teal OKLCH palette, semantic token usage across all components. The app's visual identity is fully transformed from amber/stone to modern coral/teal.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 in an incognito window
    2. **Font check:** Right-click any text, Inspect, check computed font-family shows "Geist" (not "Inter")
    3. **Homepage:** Hero section gradient should show a subtle coral-to-teal gradient (not amber/green)
    4. **Search page (http://localhost:3000/cerca):** Click "Usa la mia posizione" or check the active state buttons -- should show teal accent (not green)
    5. **Map page (http://localhost:3000/mappa):** Location button active state should show teal accent
    6. **Overall:** Browse all pages -- no amber, orange, or green-heavy elements should be visible. The palette should feel modern and vibrant (coral primary for buttons/links, teal for accents/active states, cool gray neutrals)
    7. **Contrast:** Verify body text is readable, button text is visible on coral/teal backgrounds
    8. Check for any layout shift on page load (font swap should be seamless)
  </how-to-verify>
  <resume-signal>Type "approved" if the new design looks correct, or describe any issues (color too bright/dark, contrast problems, elements looking wrong)</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` succeeds
2. `grep -r "amber-\|stone-\|from-green\|to-green\|border-green\|bg-green\|text-green\|from-orange\|via-orange" src/ --include="*.tsx" --include="*.css"` returns zero matches
3. No `dark:` prefixed color classes remain in the 3 modified files
4. Visual inspection confirms coral/teal palette renders correctly across all pages
</verification>

<success_criteria>
- Zero old-palette color references in any component (verified by grep)
- Build passes
- User confirms the new visual identity looks correct and modern
- All interactive states (buttons, active locations) use semantic tokens
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-system-foundation/16-02-SUMMARY.md`
</output>
